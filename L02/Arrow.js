const a = 1;
const b = 2;
const add = (a, b) => {
   return a + b;
}
console.log('the sum of  $ {a} and ${b} is ' + $ {add(a, b)});
const subtract = (a, b) => {
    const result = a - b;
    return result;
}
console.log('the difference of  $ {a} and ${b} is ' + $ {subtract(a, b)});